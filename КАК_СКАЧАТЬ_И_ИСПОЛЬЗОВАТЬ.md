# КАК СКАЧАТЬ ФАЙЛЫ ИЗ РЕПОЗИТОРИЯ

## САМЫЙ ПРОСТОЙ СПОСОБ (БЕЗ GIT)

### Вариант 1: Скачать ZIP архив

1. Откройте GitHub репозиторий в браузере:
   ```
   https://github.com/arbimaker/cloude-code
   ```

2. Нажмите на ветку (Branch) и выберите:
   ```
   claude/setup-wireguard-tunnels-01Y6rMqT9vyZLwkHocmabGZ8
   ```

3. Нажмите зеленую кнопку **Code**

4. Выберите **Download ZIP**

5. Распакуйте ZIP архив

6. Скопируйте все файлы на сервер 185.244.175.90 в `C:\WireGuardSetup\`

### Вариант 2: Скопировать файлы вручную

Просто создайте файлы вручную на сервере .90 - я дам вам содержимое каждого файла.

---

## ЕСЛИ У ВАС УСТАНОВЛЕН GIT

### На Windows сервере .90:

1. Откройте PowerShell или CMD

2. Клонируйте репозиторий:
   ```cmd
   cd C:\
   git clone https://github.com/arbimaker/cloude-code.git WireGuardSetup
   ```

3. Перейдите в директорию:
   ```cmd
   cd C:\WireGuardSetup
   ```

4. Переключитесь на нужную ветку:
   ```cmd
   git checkout claude/setup-wireguard-tunnels-01Y6rMqT9vyZLwkHocmabGZ8
   ```

5. Все файлы готовы к использованию!

---

## ЧТО ДЕЛАТЬ ДАЛЬШЕ (ПОШАГОВО)

### ⚠️ ВАЖНОЕ ИЗМЕНЕНИЕ В ПОДХОДЕ!

Вы правильно заметили проблему с автоматической блокировкой файрвола!

**НОВАЯ ПОСЛЕДОВАТЕЛЬНОСТЬ (ПРАВИЛЬНАЯ):**

### ШАГ 1: Обновить BCD

На сервере .90 (основная система):

```cmd
cd C:\WireGuardSetup
UPDATE_CLONE_VHDX.bat
```

### ШАГ 2: Создать скрипты управления

```cmd
mkdir C:\BootManager

copy boot-to-clone-READY.bat C:\BootManager\boot-to-clone.bat
copy boot-to-main-READY.bat C:\BootManager\boot-to-main.bat
copy check-next-boot.bat C:\BootManager\check-next-boot.bat
```

### ШАГ 3: Скачать WireGuard installer

Скачайте установщик WireGuard:
https://download.wireguard.com/windows-client/wireguard-installer.exe

Сохраните в `C:\WireGuardSetup\wireguard-installer.exe`

### ШАГ 4: Смонтировать VHDX и скопировать файлы

```cmd
diskpart
select vdisk file="C:\VMClone\clon192-4.vhdx"
attach vdisk
list volume
exit
```

Предположим VHDX смонтирован как D:

```cmd
mkdir D:\Setup
copy C:\WireGuardSetup\wireguard-installer.exe D:\Setup\
copy C:\WireGuardSetup\wg-clone.conf D:\Setup\
copy C:\WireGuardSetup\BLOCK_SOFT_ALLOW_RDP.bat D:\Setup\
copy C:\WireGuardSetup\BLOCK_HARD_TUNNEL_ONLY.bat D:\Setup\
copy C:\WireGuardSetup\EMERGENCY_RETURN-READY.bat D:\EMERGENCY_RETURN.bat
```

Размонтировать:
```cmd
diskpart
select vdisk file="C:\VMClone\clon192-4.vhdx"
detach vdisk
exit
```

### ШАГ 5: Сгенерировать ключи WireGuard

Если WireGuard установлен на .90:
```cmd
cd C:\WireGuardSetup
generate-wireguard-keys.bat
```

Запишите:
- Приватный ключ клона
- Публичный ключ клона

Получите публичный ключ эталона (на сервере .192):
```cmd
wg show
```

### ШАГ 6: Обновить wg-clone.conf

Откройте `C:\WireGuardSetup\wg-clone.conf` в блокноте:

Замените:
- `CLONE_PRIVATE_KEY_HERE` → ваш приватный ключ клона
- `ETALON_PUBLIC_KEY_HERE` → публичный ключ эталона

Сохраните файл.

Скопируйте обновленный конфиг в VHDX:
```cmd
diskpart
select vdisk file="C:\VMClone\clon192-4.vhdx"
attach vdisk
list volume
exit

copy C:\WireGuardSetup\wg-clone.conf D:\Setup\
```

Размонтировать снова.

### ШАГ 7: Обновить WireGuard на эталоне .192

На сервере 194.31.72.192 добавьте в конфигурацию WireGuard:

```ini
[Peer]
PublicKey = <ПУБЛИЧНЫЙ_КЛЮЧ_КЛОНА>
AllowedIPs = 10.0.0.2/32
```

Перезапустите WireGuard на эталоне.

### ШАГ 8: Загрузить клон

```cmd
C:\BootManager\boot-to-clone.bat
shutdown /r /t 30
```

### ШАГ 9: Войти в клон и настроить WireGuard

1. **Подключитесь по RDP к 185.244.175.90**
   - Физически это .90, но загружен клон

2. **Установите WireGuard:**
   ```cmd
   D:\Setup\wireguard-installer.exe
   ```

3. **Импортируйте конфигурацию:**
   - Откройте WireGuard GUI
   - Import tunnel from file
   - Выберите `D:\Setup\wg-clone.conf`
   - **Activate туннель!**

4. **Проверьте IP:**
   ```cmd
   curl ifconfig.me
   ```

   **ДОЛЖНО ПОКАЗАТЬ: 194.31.72.192** ✅

5. **Проверьте туннель:**
   ```cmd
   ping 10.0.0.1
   wg show
   ```

### ШАГ 10: Включить блокировку файрвола

**ТОЛЬКО после того как туннель работает:**

```cmd
D:\Setup\BLOCK_SOFT_ALLOW_RDP.bat
```

Этот скрипт:
- ✅ Блокирует все исходящие соединения кроме WireGuard
- ✅ Весь трафик ТОЛЬКО через туннель (нет утечки IP!)
- ✅ RDP остается доступным напрямую к .90

**Проверьте снова:**
```cmd
curl ifconfig.me
```

Должно по-прежнему показывать 194.31.72.192

### ШАГ 11: Проверка работы

1. RDP к 185.244.175.90 - должен работать ✅
2. `curl ifconfig.me` - показывает 194.31.72.192 ✅
3. `ping 10.0.0.1` - работает ✅
4. Любой другой трафик идет через туннель ✅

---

## ДВА ВАРИАНТА БЛОКИРОВКИ

### Вариант 1: МЯГКАЯ БЛОКИРОВКА (рекомендуется для начала)

**Файл:** `BLOCK_SOFT_ALLOW_RDP.bat`

**Что делает:**
- ✅ Блокирует все исходящие кроме WireGuard (НЕТ утечки IP!)
- ✅ Разрешает входящий RDP напрямую к .90
- ✅ Удобно для работы

**Когда использовать:**
- Для начала работы
- Когда не настроен port forwarding на эталоне

### Вариант 2: ЖЕСТКАЯ БЛОКИРОВКА (максимальная безопасность)

**Файл:** `BLOCK_HARD_TUNNEL_ONLY.bat`

**Что делает:**
- ✅ Блокирует ВСЁ кроме туннеля
- ❌ Прямой RDP к .90 НЕ работает
- ✅ RDP только через port forwarding на эталоне

**Требует настройки на эталоне .192:**

```bash
# На эталоне .192 (Linux):
iptables -t nat -A PREROUTING -p tcp --dport 33389 -j DNAT --to-destination 10.0.0.2:3389
iptables -t nat -A POSTROUTING -j MASQUERADE
```

**Подключение:** RDP к 194.31.72.192:33389

---

## АВАРИЙНЫЙ ВОЗВРАТ

В любой момент из клона:

```cmd
C:\EMERGENCY_RETURN.bat
```

Система перезапустится в основную систему.

---

## ФАЙЛЫ В КОМПЛЕКТЕ

### Готовые к использованию:
- ✅ `UPDATE_CLONE_VHDX.bat` - Обновить BCD
- ✅ `boot-to-clone-READY.bat` - Загрузить клон
- ✅ `boot-to-main-READY.bat` - Загрузить основную
- ✅ `EMERGENCY_RETURN-READY.bat` - Аварийный возврат
- ✅ `BLOCK_SOFT_ALLOW_RDP.bat` - **НОВЫЙ** - Мягкая блокировка
- ✅ `BLOCK_HARD_TUNNEL_ONLY.bat` - **НОВЫЙ** - Жесткая блокировка
- ✅ `check-next-boot.bat` - Проверка загрузки
- ✅ `generate-wireguard-keys.bat` - Генерация ключей

### Конфигурации:
- ✅ `wg-clone.conf` - WireGuard для клона
- ✅ `wg-etalon-update.conf` - Обновление эталона

### Документация:
- ✅ `КАК_СКАЧАТЬ_И_ИСПОЛЬЗОВАТЬ.md` - Этот файл
- ✅ `MASTER_INSTRUCTIONS.md` - Полная документация
- ✅ `QUICK_REFERENCE.md` - Справка

---

## ОТВЕТ НА ВАШ ВОПРОС

> "если он блокирует все соединения кроме вайргвард то как он откроется и будет доступным?"

**ПРАВИЛЬНО!** Вы обнаружили критическую ошибку в первоначальном плане!

**РЕШЕНИЕ:**

1. **НЕ включать автоблокировку при загрузке**
2. **Загрузить клон БЕЗ блокировки**
3. **Войти по RDP к .90** (физически .90, но внутри клон)
4. **Установить WireGuard вручную**
5. **Активировать туннель**
6. **Проверить что туннель работает**
7. **ТОЛЬКО ПОТОМ включить блокировку вручную**

**Два варианта блокировки:**
- **МЯГКАЯ** - RDP напрямую работает (удобно)
- **ЖЕСТКАЯ** - RDP только через эталон (максимальная безопасность)

---

## НАЧНИТЕ С ЭТОГО

1. Скачайте файлы (любым способом из начала этой инструкции)
2. Следуйте ШАГам 1-11 выше
3. Используйте МЯГКУЮ блокировку для начала
4. Позже можете перейти на ЖЕСТКУЮ (если нужно)

**ВАЖНО:** Не добавляйте скрипты блокировки в автозапуск! Запускайте вручную.
